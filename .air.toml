root = "."
testpaths = ["./..."]
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/server"
  bin = "tmp/main"
  full_bin = "tmp/main"
  include_ext = ["go"]
  include_dir = ["cmd", "internal", "pkg", "config"]
  exclude_dir = ["tmp", ".git"]